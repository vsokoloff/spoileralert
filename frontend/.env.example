# ── Spoiler Alert frontend environment ───────────────────────────────────────
# Copy to ".env" for local overrides, or set these in your host (Render/Vercel).

# Your backend's public URL. REQUIRED for the deployed web/mobile build.
# Example (Render): https://spoileralert-backend.onrender.com
# Leave UNSET for local dev — Vite proxies /api to http://localhost:8000.
VITE_API_URL=
